---
apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  labels:
    app: toolbox
spec:
  containers:
  - image: ghcr.io/kcirrr/docker-toolbox:latest
    name: toolbox
    command:
    - "sleep"
    - "86400"
