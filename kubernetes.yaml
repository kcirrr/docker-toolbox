apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  labels:
    app: toolbox
spec:
  containers:
  - image: ghcr.io/kcirrr/toolbox:latest
    command:
      - "sleep"
      - "86400"
    imagePullPolicy: IfNotPresent
    name: toolbox
  restartPolicy: Always
