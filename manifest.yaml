---
apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  labels:
    app: toolbox
spec:
  containers:
  - image: kcir/toolbox:latest
    command:
      - "sleep"
      - "86400"
    imagePullPolicy: IfNotPresent
    name: toolbox
  restartPolicy: Always
